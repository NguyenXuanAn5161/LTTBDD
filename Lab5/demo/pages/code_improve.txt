{/* code cai thien nhung chua ok! phía dưới code text chọn màu*/}
        {/* <View style={{ justifyContent: "center", alignItems: "center" }}>
          <View style={styles.boxColor}>
            {Object.keys(sourcePhoneImageColor).map((color) => (
              <Pressable
                key={color}
                style={[styles.boxColor, { backgroundColor: color }]}
                onPress={() => changePhoneColor(color)}
              />
            ))}
          </View>
        </View> */}

// navigation.setParams({ post: phoneColor });
            // navigation.goBack();